services:
  joni-onboard:
    image: joni:local
    stdin_open: true
    tty: true
    volumes:
      - ${HOME}/.joni:/home/node/.joni
    environment:
      HOME: /home/node
      TERM: xterm-256color
    entrypoint: ["node", "dist/index.js", "onboard"]
